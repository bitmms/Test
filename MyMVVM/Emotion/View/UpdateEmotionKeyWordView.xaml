<Window x:Class="MyMVVM.Emotion.View.UpdateEmotionKeyWordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyMVVM.Broadcast.View"
        mc:Ignorable="d"
        Title="UpdateEmotionKeyWordView"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="/Common/Images/logo.ico"
        ResizeMode="NoResize"
        Width="562"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        >


    <!--  仅在本页面生效的样式  -->
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml" />
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Padding="60,20,60,20" Style="{DynamicResource BroadcastFromStyle}">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="60" />
            </Grid.RowDefinitions>

            <TextBlock
                 Grid.Row="0"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 FontSize="24"
                 Text="AI智能情绪分析（以空格分割）" />

            <Grid Grid.Row="1"
                  MinHeight="60"
                  MaxHeight="300">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="330" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center">
                    <TextBlock FontSize="20" Text="关键字" />
                </Grid>

                <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                    <hc:TextBox
                        x:Name="keyword_text"
                     Width="330"
                     MinHeight="50"
                     MaxHeight="150"
                     Margin="0,5,0,5"
                     hc:InfoElement.ShowClearButton="True"
                     Background="Transparent"
                     BorderBrush="#2196f3"
                     FontSize="18"
                     Foreground="#000000"
                     TextWrapping="Wrap" />
                </ScrollViewer>

            </Grid>

            <Grid Grid.Row="2">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Button
                 Grid.Column="1"
                 Click="Button_Click"
                 CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                 Content="取消"
                 Style="{DynamicResource MaterialDesignPaperButton}" />

                <Button
                 Grid.Column="3"
                 Click="Button_Click_1"
                 CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                 Content="确定"
                 Style="{DynamicResource MaterialDesignRaisedLightButton}" />

            </Grid>

        </Grid>

    </Border>

</Window>
