<Window x:Class="MyMVVM.Emotion.View.PlayAudioView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyMVVM.Emotion.View"
        mc:Ignorable="d"
        Title="PlayAudioView"
        Width="450"
        Height="380"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        MouseDown="Window_MouseDown"
        WindowStartupLocation="CenterScreen"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!--  仅在本页面生效的样式  -->
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml" />
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Style="{DynamicResource UserCdrMain}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0">
                <Grid Grid.Row="0">
                    <TextBlock
                        Margin="20,18,0,0"
                        FontFamily="楷体"
                        FontSize="24"
                        Style="{DynamicResource ButtonBlockStyle}"
                        Text="详细信息" />
                    <Button
                        x:Name="btnClose"
                        Grid.Column="2"
                        Height="50"
                        Margin="15,0,0,0"
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="BtnClose_OnClick">
                        <materialDesign:PackIcon
                            Width="45"
                            Height="45"
                            Foreground="Black"
                            Kind="CloseCircleOutline" />
                    </Button>
                </Grid>
            </Border>

            <Border Grid.Row="1" Padding="20 0 20 20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="2*" />
                    </Grid.RowDefinitions>

                    <!-- 媒体控件 -->
                    <Border Grid.Row="0">
                        <MediaElement
                            x:Name="mediaElement"
                            SpeedRatio="1"
                            LoadedBehavior="Manual"
                            MediaOpened="MediaElement_MediaOpened"
                            MediaEnded="MediaElement_MediaEnded" />
                    </Border>

                    <!-- 主叫 -->
                    <Grid Grid.Row="1">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="290" />
                        </Grid.ColumnDefinitions>

                        <Grid
                            Grid.Column="0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <TextBlock FontSize="20" Text="主叫" />
                        </Grid>

                        <TextBox
                            x:Name="from_number_textblock"
                            Grid.Column="2"
                            Margin="0,5,0,5"
                            Background="Transparent"
                            BorderBrush="#2196f3"
                            FontSize="18"
                            Foreground="#000000"
                            IsReadOnly="True" />
                    </Grid>

                    <!-- 被叫 -->
                    <Grid Grid.Row="2">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="290" />
                        </Grid.ColumnDefinitions>

                        <Grid
                            Grid.Column="0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <TextBlock FontSize="20" Text="被叫" />
                        </Grid>

                        <TextBox
                            x:Name="to_number_textblock"
                            Grid.Column="2"
                            Margin="0,5,0,5"
                            Background="Transparent"
                            BorderBrush="#2196f3"
                            FontSize="18"
                            Foreground="#000000"
                            IsReadOnly="True" />
                    </Grid>

                    <!-- 时间 -->
                    <Grid Grid.Row="3">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="290" />
                        </Grid.ColumnDefinitions>

                        <Grid
                            Grid.Column="0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <TextBlock FontSize="20" Text="时间" />
                        </Grid>

                        <TextBox
                            x:Name="call_time_textblock"
                            Grid.Column="2"
                            Margin="0,5,0,5"
                            Background="Transparent"
                            BorderBrush="#2196f3"
                            FontSize="18"
                            Foreground="#000000"
                            IsReadOnly="True" />
                    </Grid>

                    <!-- 关键词 -->
                    <Grid Grid.Row="4">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="290" />
                        </Grid.ColumnDefinitions>

                        <Grid
                            Grid.Column="0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <TextBlock FontSize="20" Text="关键词" />
                        </Grid>

                        <TextBox
                            x:Name="keyword_textblock"
                            Grid.Column="2"
                            Margin="0,5,0,5"
                            Background="Transparent"
                            BorderBrush="#2196f3"
                            FontSize="18"
                            Foreground="#000000"
                            IsReadOnly="True" />
                    </Grid>

                    <!-- 播放进度条 -->
                    <Grid Grid.Row="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="290" />
                        </Grid.ColumnDefinitions>

                        <Grid
                            Grid.Column="0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                            <TextBlock FontSize="20" Text="播放录音" />
                        </Grid>

                        <Border Grid.Column="2"
                                BorderThickness="1"
                                Margin="0 5 0 5"
                                CornerRadius="5"
                                Background="Transparent"
                                BorderBrush="#2196f3"
                                Padding="10 0 10 0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Slider
                                    Grid.Row="0"
                                    x:Name="sliderProgress"
                                    Minimum="0"
                                    Maximum="100"
                                    ValueChanged="SliderProgress_ValueChanged"
                                    Thumb.DragStarted="SliderProgress_DragStarted"
                                    Thumb.DragCompleted="SliderProgress_DragCompleted"
                                    VerticalAlignment="Center" />
                                <Border Grid.Row="2" Padding="6 0 0 0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="4*" />
                                            <ColumnDefinition Width="4*" />
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" VerticalAlignment="Top" HorizontalAlignment="Left">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock
                                                    Grid.Column="0"
                                                    x:Name="txtCurrentTime"
                                                    Text="00:00"
                                                    TextAlignment="Left"
                                                    FontSize="16"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center" />
                                                <TextBlock
                                                    Grid.Column="1"
                                                    Text="/"
                                                    FontSize="16"
                                                    TextAlignment="Left"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Margin="5 0 5 0" />
                                                <TextBlock
                                                    Grid.Column="2"
                                                    x:Name="txtTotalTime"
                                                    Text="00:00"
                                                    FontSize="16"
                                                    TextAlignment="Right"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                        <Border Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Top">
                                            <Button
                                                x:Name="btnPlayPause"
                                                Width="90"
                                                Height="30"
                                                Content="播放"
                                                Click="BtnPlayPause_Click" />
                                        </Border>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- 播放时长、播放控件 -->

                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>