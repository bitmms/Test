<Window x:Class="MyMVVM.Setting.View.SettingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyMVVM.Setting.View"
        mc:Ignorable="d"
        Title="SettingView"
        Width="800"
        Height="450"
        Background="Transparent"
        Icon="/Common/Images/icon1.ico"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        >
    <Border Background="White">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Background="#2E333A">
                <Menu>
                    <Menu.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </Menu.ItemsPanel>
                    <MenuItem
                        Click="MenuItem_Click"
                        Header="主题设置"
                        Tag="ThemeSettings"
                        Template="{StaticResource Menu_Template}">
                        <MenuItem.Icon>
                            <Image Source="/Common/Images/themesetting.png" Stretch="Uniform" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <!--<MenuItem
                        Click="MenuItem_Click"
                        Header="用户管理"
                        Tag="UserManagement"
                        Template="{StaticResource Menu_Template}">
                        <MenuItem.Icon>
                            <Image Source="/Common/Images/usermanage.png" Stretch="Uniform" />
                        </MenuItem.Icon>
                    </MenuItem>-->
                    <MenuItem
                        x:Name="WIFIMenuItem"
                        Click="MenuItem_Click"
                        Header="基站设置"
                        Tag="WiFiManagement"
                        Template="{StaticResource Menu_Template}">
                        <MenuItem.Icon>
                            <Image Source="/Common/Images/wifisetting.png" Stretch="Uniform" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem
                        Click="MenuItem_Click"
                        Header="退出"
                        Tag="Logout"
                        Template="{StaticResource Menu_Template}">
                        <MenuItem.Icon>
                            <Image Source="/Common/Images/logout.png" Stretch="Uniform" />
                        </MenuItem.Icon>
                    </MenuItem>
                </Menu>
            </Grid>

            <Grid Grid.Column="1">
                <ContentControl x:Name="ContentArea" />
            </Grid>
        </Grid>
    </Border>
</Window>