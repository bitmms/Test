<UserControl x:Class="MyMVVM.WIFI.View.WIFIView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyMVVM.WIFI.View"
             mc:Ignorable="d" 
             Width="1480"
             Height="760"
             >


    <Border Padding="0 0 100 0">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="9*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <WrapPanel VerticalAlignment="Center">
                    <Button
                        Width="150"
                        Height="45"
                        Margin="20,0,0,0"
                        Background="#7EC0EE"
                        Content="扫描基站"
                        Command="{Binding ScanWIFICommand}"
                        Style="{DynamicResource DispatchButton}"
                        FontSize="18"
                        Foreground="Black" />
                </WrapPanel>
            </Grid>

            <Grid Grid.Row="1">
                <ItemsControl ItemsSource="{Binding WIFIDateList}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="9" Rows="10" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding WifiCommand}" Style="{DynamicResource GroupButtonStyle}" Background="{Binding Background}">
                                <StackPanel>
                                    <TextBlock HorizontalAlignment="Center" Text="{Binding WIFIName}" />
                                    <TextBlock HorizontalAlignment="Center" Text="{Binding WIFIIP}" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Grid>

    </Border>

</UserControl>