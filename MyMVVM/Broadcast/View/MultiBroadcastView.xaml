<Window x:Class="MyMVVM.Broadcast.View.MultiBroadcastView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyMVVM.Broadcast.View"
        mc:Ignorable="d"
        Title="MultiBroadcastView" 
        Width="562"
        SizeToContent="Height"
        Icon="/Common/Images/logo.ico"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Background="Transparent"
        >


    <Window.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Padding="60,0,60,0" Style="{DynamicResource BroadcastFromStyle}">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="60" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="60" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <TextBlock Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Text="设置多方广播" />
            </Grid>

            <Grid Grid.Row="1">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="330" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      >
                    <TextBlock  FontSize="20" Text="广播类型" />
                </Grid>

                <ComboBox Grid.Column="2"
                          Foreground="#000000"
                          Background="Transparent"
                          BorderBrush="#2196f3"
                          FontSize="18"
                          Width="330"
                          MinHeight="50"
                          MaxHeight="150"
                          Margin="0 5 0 5"
                          hc:DropDownElement.ConsistentWidth="False" 
                          ItemsSource="{Binding BroadcastTypeList}"
                          SelectedItem="{Binding NowSelectedBroadCastType}"
                          />

            </Grid>

            <Grid Grid.Row="2" MinHeight="60" MaxHeight="300" Visibility="{Binding IsShowTTS, Converter={StaticResource BooleanToVisibilityConverter}}">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="330" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      >
                    <TextBlock  FontSize="20" Text="广播内容" />
                </Grid>

                <ComboBox
                    Grid.Column="2"
                    Width="330"
                    MinHeight="50"
                    MaxHeight="150"
                    Margin="0,5,0,5"
                    hc:DropDownElement.ConsistentWidth="False"
                    Background="Transparent"
                    BorderBrush="#2196f3"
                    FontSize="18"
                    Foreground="#000000"
                    ItemsSource="{Binding TTSList}"
                    SelectedItem="{Binding NowSeletedTTSItem}" />

            </Grid>

            <Grid Grid.Row="2" MinHeight="60" MaxHeight="180" Visibility="{Binding IsShowMusic, Converter={StaticResource BooleanToVisibilityConverter}}">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="330" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      >
                    <TextBlock  FontSize="20" Text="选择音乐" />
                </Grid>

                <hc:CheckComboBox Grid.Column="2"
                                  Foreground="#000000"
                                  Background="Transparent"
                                  BorderBrush="#2196f3"
                                  FontSize="18"
                                  Width="330"
                                  MinHeight="50"
                                  MaxHeight="150"
                                  Margin="0 5 0 5"
                                  hc:ListBoxAttach.SelectedItems="{Binding SelectedMusicList}" 
                                  ItemsSource="{Binding MusicList}" 
                                  hc:InfoElement.ShowClearButton="True" 
                                  ShowSelectAllButton="True"
                                  hc:InfoElement.Necessary="True" 
                  />

            </Grid>

            <Grid Grid.Row="2" Height="0" Visibility="{Binding IsManual, Converter={StaticResource BooleanToVisibilityConverter}}">
            </Grid>


            <Grid Grid.Row="3"
                  Visibility="{Binding IsShowMusic, Converter={StaticResource BooleanToVisibilityConverter}}"
                  >

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="330" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      >
                    <TextBlock  FontSize="20" Text="播放次数" />
                </Grid>

                <ComboBox Grid.Column="2"
                          Foreground="#000000"
                          Background="Transparent"
                          BorderBrush="#2196f3"
                          FontSize="18"
                          Width="330"
                          MinHeight="50"
                          MaxHeight="150"
                          Margin="0 5 0 5"
                          hc:DropDownElement.ConsistentWidth="False" 
                          ItemsSource="{Binding PlayCountList}"
                          SelectedItem="{Binding NowSeletedPlayCount}"
                          />

            </Grid>

            <Grid Grid.Row="3"
                  Visibility="{Binding IsShowTTS, Converter={StaticResource BooleanToVisibilityConverter}}"
                  >

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="330" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      >
                    <TextBlock  FontSize="20" Text="播放次数" />
                </Grid>

                <ComboBox Grid.Column="2"
                          Foreground="#000000"
                          Background="Transparent"
                          BorderBrush="#2196f3"
                          FontSize="18"
                          Width="330"
                          MinHeight="50"
                          MaxHeight="150"
                          Margin="0 5 0 5"
                          hc:DropDownElement.ConsistentWidth="False" 
                          ItemsSource="{Binding PlayCountList}"
                          SelectedItem="{Binding NowSeletedPlayCount}"
                          />

            </Grid>


            <Grid Grid.Row="4">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1"
                                Command="{Binding CancelButtonCommand}"
                                CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                Content="取消"
                                Style="{DynamicResource MaterialDesignPaperButton}" />

                <Button Grid.Column="3"
                                Command="{Binding ConfirmButtonCommand}"
                                CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                Content="确定"
                                Style="{DynamicResource MaterialDesignRaisedLightButton}" />

            </Grid>

        </Grid>

    </Border>

</Window>